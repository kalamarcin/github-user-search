import { render, screen } from '@testing-library/react'
import User from '../User/User'

describe('User Component', () => {
  it('Schuld render User component', () => {
    render(<User />)
    
  })
})
